# Frontend talks to the backend via the same-origin /api/* proxy
# (handled by Cloudflare Pages Functions in /functions/api/[[path]].ts).
# Keep this empty so lib/api.ts falls back to relative URLs and the auth
# cookie stays first-party. DO NOT set this to the absolute Render URL.
VITE_API_BASE_URL=
